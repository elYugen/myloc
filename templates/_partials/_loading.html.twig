<div id="loading">
    <img src="{{ asset('img/logo.png') }}" alt="Logo" />
    <img src="{{ asset('img/loading.gif') }}" alt="Loading..." class="load" />
</div>

<style>
    #loading {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: white;
        display: flex;
        justify-content: center;
        flex-direction:column;
        align-items: center;
        z-index: 9999;
    }

    #load {
        width: 50%;
    }
</style>
